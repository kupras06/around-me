profiles
- Verify the table has avatar_url text nullable.
- Verify the table has is_admin boolean nullable/default false.

submissions
- Add a relationship from submissions.user_id to profiles.id if admin review should show submitter profile data.
- If submissions are meant to join creators directly, add creator_id referencing creators.id instead of relying on a missing creators relationship.

linked_accounts
- No app code should use this table for auth provider state. Provider state now comes from Supabase Auth identities.
